  
  
   <!-- Carousel
    ================================================== -->
    <div id="myCarousel" class="carousel slide">
      <div class="carousel-inner">
        <div class="item active">
          <%= img= image_tag "banner1.jpg" %>

            <div class="container">
              <div class="carousel-caption">
                <h1>Wecome to Real Apparel</h1>
                <p>A marketplace for crowdfunded custom apparel</p>
                <a class="btn btn-primary" href="#">Create a Campaign</a>
              </div>
            </div>
          </div>
        <div class="item">
          <%= img= image_tag "banner2.png" %>
          <div class="container">
            <div class="carousel-caption">
              <h1>Create a Fundraiser</h1>
              <p>Pick a product, set a price & target and you can choose to upload a design</p>
              <a class="btn btn-primary" href="campaigns/new">Create New</a>
            </div>
          </div>
        </div>
        <div class="item">
          <%= img= image_tag "banner3.png" %>
          <div class="container">
            <div class="carousel-caption">
              <h1>Start Fundraising! It's that easy!</h1>
              <p>A campaign can run for a maximum of 30 days and for every shirt sold you will raise a MINIMUM $5</p>
              <a class="btn btn-primary" href="#">Get Started!</a>
            </div>
          </div>
        </div>
      </div>
      <a class="left carousel-control" href="#myCarousel" data-slide="prev">&lsaquo;</a>
      <a class="right carousel-control" href="#myCarousel" data-slide="next">&rsaquo;</a>
    </div><!-- /.carousel -->



<div class='container'>
  <div class='row'>
    <ul class="thumbnails">
      <% @campaigns.each do |campaign| %>
        <li class='span4'>
          <div class='thumbnail'>
            <%= image_tag campaign.reward.reward_image.url(:small) %>
            <div class="caption">
              <h3><%= link_to campaign.name, campaign_path(campaign) %></h3>
              <h4> Finishes: <%= campaign.finish_time %></h4>
            </div>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</div>

<%- model_class = Campaign -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:name) %></th>
      <th><%= model_class.human_attribute_name(:description) %></th>
      <th><%= model_class.human_attribute_name(:start_time) %></th>
      <th><%= model_class.human_attribute_name(:finish_time) %></th>
      <th><%= model_class.human_attribute_name(:donation_amount) %></th>
      <th><%= model_class.human_attribute_name(:reward_id) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @campaigns.each do |campaign| %>
      <tr>
        <td><%= link_to campaign.id, campaign_path(campaign) %></td>
        <td><%= campaign.name %></td>
        <td><%= campaign.description %></td>
        <td><%= campaign.start_time %></td>
        <td><%= campaign.finish_time %></td>
        <td><%= campaign.donation_amount %></td>
        <td><%= campaign.reward_id %></td>
        <td><%=l campaign.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_campaign_path(campaign), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      campaign_path(campaign),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_campaign_path,
            :class => 'btn btn-primary' %>
